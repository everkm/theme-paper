/** Everkm content directory (`dir` in posts / post_neighbors queries). */
export const POSTS_CONTENT_DIR = "/";

/** Virtual URL base path for the posts list (no trailing slash). */
export const POSTS_PATH = "/posts";

/** Virtual URL for the posts list index page. */
export const POSTS_INDEX_URL = `${POSTS_PATH}/index.html`;
